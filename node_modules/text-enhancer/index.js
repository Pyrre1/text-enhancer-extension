import { TextEnhancerController } from "./src/controller/TextEnhancerController.js"

/**
 * Creates a new instance of the Text Enhancer controller. *might change to Enhancer later*
 * 
 * @param {Object} config - Configuration object for the text Enhancer.
 * @returns {TextEnhancerController} An instance of the TextEnhancerController.
 */

export function createTextEnhancer(config) {
  return new TextEnhancerController(config)
}