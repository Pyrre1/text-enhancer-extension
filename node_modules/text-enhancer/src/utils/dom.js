/**
 * This method abstracts the logic of querying multiple selectors from a root element.
 * allowing the controller/model to operate on a clean array of matched elements.
 */
export function getTargetElements(selectors, root) {
  const query = selectors.join(', ')
  return Array.from(root.querySelectorAll(query))
}